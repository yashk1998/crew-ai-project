---
collect_indian_startup_content:
  description: |-
    Auto-discover the most trending stories of the last 24-48 hours that matter to **Indian early-stage SaaS/B2B founders (pre-seed → series-A)**, using BOTH the Tavily web search and the ambient RSS feeds.

    **Steps (do all of them in this order):**

    1. **Tavily targeted search:** Call `Tavily Search` with one of these queries (pick the one that feels most likely to surface fresh, India-specific founder-relevant stories TODAY):
       - 'Indian startup news this week'
       - 'India early-stage founder news'
       - 'India SaaS funding this week'
       - 'Indian startup ecosystem trending'

    2. **RSS ambient sweep:** Call `rss_feed_collector` with no arguments (default Indian startup feeds, 24-hour window). If fewer than 3 items come back, retry with `hours_back=48`. The RSS tool internally validates URLs (drops dead links), so its output is pre-filtered.

    3. **Merge and dedupe:** Combine both result sets. Deduplicate by URL. Drop items missing a title or url.

    4. **Map to RawItems:** title, brief_description (1-2 sentences from the tool snippet, copy verbatim — do NOT rewrite), url (copy character-for-character), source (use the source from the tool output), published (ISO timestamp from tool, or empty string).

    5. **Cap output at 30 items, minimum 3.**

    **Hard rules:**
    - NEVER invent items. Every RawItem must trace to an actual tool result.
    - NEVER modify a URL. Copy URLs exactly. The downstream brief publishes them — a 404 destroys page credibility.
    - If a URL looks malformed (no protocol, no domain), drop the item entirely rather than try to fix it.
    - Do NOT fetch full article bodies.
  expected_output: A RawItemBatch with 3-30 deduplicated items spanning Tavily
    search results and RSS feeds, each with title, 1-2 sentence brief_description,
    a verified working url, source name, and published timestamp (or empty).
  agent: source_scout_agent

classify_and_cluster:
  description: |-
    Analyze each RawItem and organize them into topic clusters, scoring relevance against the locked persona: **Indian early-stage SaaS/B2B founders (pre-seed → series-A)**.

    **For each content item:**
    1. Classify with startup stage, content pillar, india_specific flag, and persona_fit_score (0.0-1.0).
    2. Group semantically similar items into clusters (same event covered by multiple outlets, related sub-topics).
    3. Name each cluster with the most specific, actionable canonical_topic title.

    **Persona-fit scoring guidance:**
    - Genuinely useful for Indian early-stage founders (specific ₹ amounts, named Indian cos, frameworks, contrarian takes): persona_fit_score 0.7-1.0
    - Adjacent (Indian startup news without direct founder takeaway, US content with India relevance): 0.4-0.7
    - Off-pillar or generic ecosystem chatter: 0.0-0.4

    **Classification framework:**
    - Stages: seed, series-a, series-b+, growth, exits, irrelevant
    - Pillars: fundraising, gtm, hiring, product, founder_psych, exits, regulation
    - india_specific: true if the story is India-rooted (Indian co, Indian regulator, Indian VC), false otherwise

    **Output requirements:**
    - Only include clusters with avg persona_fit_score ≥ 0.3
    - Each cluster has 1-5 related items
    - Base classifications ONLY on actual item content — never invent details the source lacks
  expected_output: A TopicClusterBatch covering the persona-relevant content,
    excluding clusters with stage='irrelevant' or avg persona_fit_score < 0.3.

  agent: content_classifier_and_topic_analyzer
  context:
  - collect_indian_startup_content

validate_and_rank:
  description: |-
    Evaluate every TopicCluster using the scoring rubric and return the top 3 clusters most likely to drive Instagram follower growth for **Indian early-stage SaaS/B2B founders**.

    **Scoring criteria (weighted):**
    1. Indian Startup Relevance (30%) — How specific to the Indian startup ecosystem
    2. Actionability (25%) — Can a pre-seed/Series A founder immediately apply this?
    3. Stage Fit (25%) — Relevant for seed/Series A founders specifically
    4. Content Quality (20%) — Substance and credibility of source material

    **Process:**
    1. Score each cluster 1-10 on each criterion
    2. final_score = relevance×0.3 + actionability×0.25 + stage×0.25 + quality×0.2
    3. Apply minimum thresholds: final_score ≥ 6.0 AND avg persona_fit_score ≥ 0.6
    4. Return top 3 ranked by final_score (fewer if fewer pass thresholds)

    **Score breakdown format (must show actual numbers):**
    "relevance=8.0×0.3 + actionability=7.5×0.25 + stage=8.5×0.25 + quality=7.0×0.2 = 7.75"
  expected_output: A RankedTopicBatch with up to 3 topics, ranked by final_score,
    each showing the actual numerical computation in score_breakdown.
  agent: topic_ranking_and_selection_specialist
  context:
  - classify_and_cluster

choose_format:
  description: |-
    Of the 3 ranked topics, assign formats with this MANDATORY mix: **exactly 1 carousel + exactly 2 reels in narrative progression.** No stories, no static.

    **Carousel pick (1 topic):**
    - Identify which one of the 3 topics has the most TACTICAL, framework, multi-step, or step-by-step content. That's your carousel topic.
    - Carousels drive saves; saves drive algorithmic reach. Set expected_primary_metric = "saves".
    - rubric_match must start with "Carousel — " followed by the rubric category that applies.

    **Reel pair (the remaining 2 topics, in narrative progression):**
    - The other two become reels. Order them so a clear arc connects them:
      * **Part 1 (Setup)** — surfaces the tension, names the dilemma, plants the question, ends with explicit Part-2 teaser.
      * **Part 2 (Payoff)** — opens with "Yesterday I asked..." callback, delivers the framework / numbers / contrarian take.
    - The two topics should share a thread (same audience pain, same domain, or same protagonist company). If they don't naturally pair, order them so Part 1 = broader/setup and Part 2 = sharper/payoff.
    - Both reels: expected_primary_metric = "watch_time".
    - rubric_match for the two reels must be exactly "Reel — Part 1 (Setup)" and exactly "Reel — Part 2 (Payoff)".
    - The `reasoning` field for each reel MUST state the narrative bridge: "Part 1 raises X; Part 2 answers it with Y."

    **Rubric for the carousel (cite which one applies in rubric_match):**
    - Tactical content (frameworks, checklists, playbooks)
    - Tutorial / how-to with visual density
    - Multi-step decision framework
    - Detailed breakdown of an Indian co's strategy

    **HARD RULES:**
    1. Output exactly 3 FormatRecommendations: 1 with format="carousel", 2 with format="reel".
    2. expected_primary_metric: carousel→"saves", reel→"watch_time" (both reels).
    3. rubric_match strings must follow the patterns above ("Carousel — <category>", "Reel — Part 1 (Setup)", "Reel — Part 2 (Payoff)") — the brief generator reads these to know which reel is which.
    4. Reasoning for the reels must explicitly bridge Part 1 → Part 2.
  expected_output: A FormatRecommendationBatch with exactly 3 recommendations —
    one carousel (saves) and two reels in narrative progression (watch_time), each
    citing its rubric match and the reels naming their narrative bridge.
  agent: instagram_content_format_specialist_for_b2b_founder_audiences
  context:
  - validate_and_rank

generate_schema_compliant_instagram_briefs:
  description: |-
    Produce EXACTLY 3 Instagram briefs for Indian early-stage SaaS/B2B founders, with the format mix the strategist assigned: 1 carousel + 2 reels in progression (the strategist labels each reel "Reel — Part 1 (Setup)" or "Reel — Part 2 (Payoff)" in rubric_match — read it). Output is **MARKDOWN TEXT** following the template below — NOT JSON. The markdown IS the final deliverable: it renders directly on the CrewAI dashboard. Persona is locked: awareness-driven content for the page's growth from 500K to 1M followers in 6 months.

    **Source-grounding rule (most important):** Every URL you cite MUST come from the upstream RankedTopic.items list. Copy URLs character-for-character. NEVER invent URLs. NEVER use example.com.

    **Output the briefs using EXACTLY this markdown template (one block per brief, separated by `---`):**

    ```
    ## Brief #N: <topic line>

    > **Thesis:** <one sharp sentence on why this content exists>

    **Audience:** <specific subaudience, e.g. "Pre-seed B2B SaaS founders negotiating their first cap table">
    **Why now:** <what makes this timely THIS WEEK; cite the source date>
    **Format:** <Carousel | Reel — Part 1 (Setup) | Reel — Part 2 (Payoff)>
    **Specs:** <e.g. "8-10 slides | 4:5 | Slide 1 hook | 5 hashtags" or "5-7 shots × 30-45s total | 9:16 | Hook in first 1.5s | DM trigger in final shot">
    **Fact-check:** <Verified | Evergreen | Unverified> — <one-sentence note>

    ### Sources
    - [<title>](<url>) — <source name>
    - [<title>](<url>) — <source name>

    ### Hooks
    **A · Specific number / loss-aversion** — <hook text, ≤15 words>
      _Why it works: <1 sentence tagged to format's primary metric>_
    **B · Contrarian-but-defensible** — <hook text>
      _Why it works: <1 sentence>_
    **C · Insider language / pattern recognition** — <hook text>
      _Why it works: <1 sentence>_

    ### Slide / Shot Blueprint
    | # | Slide / Shot | Visual + Design | Headline / On-screen | Voiceover | Transition |
    |---|---|---|---|---|---|
    | 1 | <Slide 1 / Shot 1> | <visual concept; design: hex colors, typography, layout> | <≤8 words carousel; ≤6 words reel sticker> | <empty for carousel; required + ≠ headline for reel> | <cut / whip / match-cut / N/A for carousel> |
    | 2 | ... | ... | ... | ... | ... |

    For CAROUSEL: 8-10 slides. Slide 1 = hook, Slide 2 = stakes, Slides 3-N = framework (one step per slide), final slide = summary + Follow CTA. Voiceover column usually empty.
    For REEL: 5-7 shots, 30-45s total, 9:16. Shot 1 = visual hook in <1.5s. Final shot = CTA + DM trigger. **Voiceover MUST differ from on-screen text** (sticker is ≤6 words; voiceover is 1-3 conversational sentences, founder-to-founder, ending each shot with a tease that pulls to the next).
    For REEL — PART 1 (SETUP): plant the question, surface the tension, end with a teaser explicitly previewing Part 2 ("Tomorrow: the 3 numbers that decide it.").
    For REEL — PART 2 (PAYOFF): open with "Yesterday I asked..." callback, then deliver the framework / numbers / contrarian take. Reference Part 1 in the caption.

    ### Captions (3 drafts)

    **Long (140-180 words):**
    > <story-led primary caption with line breaks every 1-2 sentences; first line survives Instagram's "Read More" cut at ~125 chars>

    **Medium (60-90 words):**
    > <punchier, framework-led; no story setup; A/B alternative or cross-platform variant>

    **Short (≤30 words):**
    > <pure hook + CTA; for reels where the video carries the message>

    ### Hashtags (tiered, 5-7 total, no #)
    - **Broad (1-2):** <e.g. IndianStartups, SaaS>
    - **Niche (2-3):** <e.g. IndianSaaS, B2BSaaSIndia, BangaloreFounders>
    - **Community / branded (1):** <branded tag or FoundersOfIndia>
    - **Trend (0-1):** <only if a trending tag legitimately matches; else omit>

    ### CTAs
    - **Primary:** <matched to format's primary metric — Save / Watch Again / Share with co-founder>
    - **Comment-driver:** <specific low-friction question, e.g. "Which one are you most guilty of? 1, 2, or 3?">
    - **DM-trigger:** <reel only — e.g. "DM 'CAP' for the cap-table template">
    - **Follow:** <used in pinned comment + final slide/shot, e.g. "Follow for daily breakdowns of Indian early-stage playbooks">

    ### Audio
    <Reel: trending sound name + BPM + mood + a fallback. Carousel: "No audio (silent IG carousel)" + Story-tease audio note.>

    ### Posting Strategy
    - **Day + time (IST):** <e.g. Tue 9:00 AM IST — founders pre-stand-up; justify from topic if applicable>
    - **Cross-promo:** <Story tease 2-4 hours before; what the tease shows>
    - **Sequencing:** <for the reel pair — Part 1 publishes 24h before Part 2; Part 2 caption opens with "Yesterday I asked..." callback>

    ### Engagement Playbook (first 60 minutes)
    - **Pinned comment (verbatim, ≤200 chars):** <framework summary + DM trigger>
    - **Prepared reply lines (3):**
      1. Agreement amplifier — <line>
      2. Gentle disagreement — <line>
      3. Follow-up question to extend the thread — <line>
    - **Outbound engagement (3 accounts to comment on within 30 min):** <named accounts in niche>
    - **Story amplifier (30 min after feed post):** <slide concept + poll/question sticker>
    - **Follow-up post idea (tomorrow's queue):** <the natural next post extending this topic>

    ---
    ```

    **NORTH STAR — what these briefs are optimised for:**

    The KPI is page growth from 500K → 1M followers in 6 months. The two compounding signals are AWARENESS (reach to non-followers via Explore + recommended feeds) and LIKES (the dominant low-friction signal Instagram's algorithm reads to push posts to more non-follower feeds). Every line of every brief must pass at least one of:
    (1) Earns a LIKE in under 3 seconds — via a quotable line, visual punch, or stab-of-recognition insight a founder taps to validate.
    (2) Earns a SAVE — via a tactical framework, numeric breakdown, or checklist worth keeping.
    (3) Earns a SHARE — via a contrarian-and-defensible take a founder forwards to a co-founder, investor, or hiring manager.
    Lines that earn none of these are wasted impressions. Cut them or rewrite — never pad to length.

    **SUBSTANCE FLOOR — minimums every brief MUST clear (no exceptions):**
    - **Carousel:** at least 8 slides (target 9-10). Every slide carries a distinct insight, step, or stat. No filler / transition slides.
    - **Reel:** at least 5 shots (target 6-7). Every shot is a distinct beat (question, twist, evidence, framework, payoff). Hook in <1.5s. Final shot = CTA + DM trigger.
    - **All 3 caption drafts** present and complete: long (140-180w), medium (60-90w), short (≤30w). No skipping. Long caption's first line survives the "Read More" cut at ~125 chars.
    - **Specificity floor per brief:** ≥ 3 named Indian companies, ≥ 2 specific ₹ amounts or percentages, ≥ 1 named framework / playbook / role / named person.
    - **All 5 engagement-playbook components** filled with concrete specifics (pinned comment verbatim + 3 prepared reply lines + 3 named outbound accounts + Story amplifier slide + follow-up post idea).
    - **All 3 hook angles** filled (specific-number, contrarian-defensible, insider-language) with their own one-sentence why-it-works.
    - **All 4 CTAs** filled: primary, comment-driver, DM-trigger, follow.
    - **At least 1 real upstream URL** per brief; preferably 2-3 from the cluster.

    **HARD RULES:**
    - Output exactly 3 brief blocks: 1 carousel + 2 reels (Part 1 then Part 2). Read each FormatRecommendation.format and rubric_match to know which is which.
    - Every URL must come from the upstream cluster — never invent. If sources are thin, mark fact_check as Evergreen and frame the brief as a pattern.
    - Specific over vague: ₹ amounts, named Indian cos (Razorpay, Postman, Zoho, BharatPe, Freshworks, Smallcase, Cred, Slice, Yellow.ai, Atlan, Chargebee, Khatabook, OfBusiness, Apna, Whatfix, MoEngage, etc.), real numbers from sources.
    - For reels: every voiceover line must differ from its on-screen sticker text.
    - For the 2-reel progression: Part 1 explicitly teases Part 2 (final shot teaser); Part 2 explicitly callbacks Part 1 (opening voiceover + caption).
    - Zero motivational filler ("hustle", "grind", "rise and shine", "trust the process").
    - Every line passes the like-save-share test, or it gets cut.
    - Output is the markdown brief content directly — no preamble, no postamble, no JSON, no code fence wrapping the whole output. The markdown IS the final deliverable.
  expected_output: |-
    A markdown document containing exactly 3 brief blocks following the template — Brief 1 (Carousel), Brief 2 (Reel — Part 1 Setup), Brief 3 (Reel — Part 2 Payoff), each with: thesis, audience, why-now, format/specs, fact-check, sources, 3 hooks with reasoning, slide/shot blueprint table, three caption drafts (long/medium/short), tiered hashtags, multi-CTA stack (primary/comment/DM/follow), audio, posting strategy, and a 5-part engagement playbook (pinned comment, 3 prepared replies, outbound engagement, Story amplifier, follow-up post idea). All URLs traced to upstream cluster. Reel voiceovers punchy and different from on-screen stickers. Part 1 sets up + Part 2 pays off, with explicit cross-references between them.
  agent: senior_creative_director_for_instagram_b2b_content___schema_compliant
  context:
  - validate_and_rank
  - choose_format
